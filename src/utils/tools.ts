// 通用函数
import * as copy from './tools/copy';
import * as datetime from './tools/datetime';
import * as numbers from './tools/numbers';
import * as objects from './tools/objects';
import * as storage from './tools/storage';
import * as theme from './tools/theme';

export default {
    copy,
    datetime,
    numbers,
    objects,
    storage,
    theme,
};